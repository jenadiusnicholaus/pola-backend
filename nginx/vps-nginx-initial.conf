# ==========================================
# POLA Backend Nginx Configuration - Initial Setup (No SSL)
# ==========================================
# Usage:
# 1. Replace yourdomain.com with your actual domain
# 2. Update /path/to/your/project with your actual path
# 3. Copy: sudo cp vps-nginx-initial.conf /etc/nginx/sites-available/pola-backend
# 4. Enable: sudo ln -s /etc/nginx/sites-available/pola-backend /etc/nginx/sites-enabled/
# 5. Test: sudo nginx -t
# 6. Reload: sudo systemctl reload nginx
# 7. Get SSL: sudo certbot --nginx -d yourdomain.com
# ==========================================

# Upstream Django application (Docker containers)
upstream pola_backend {
    server 127.0.0.1:8000;
    keepalive 32;
}

# Rate limiting
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;

# HTTP Server (Initial setup - no SSL)
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;

    # Allow certbot challenges
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
        allow all;
    }

    # Static files
    location /static/ {
        alias /path/to/your/project/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Media files
    location /media/ {
        alias /path/to/your/project/media/;
        expires 7d;
        add_header Cache-Control "public";
    }

    # API endpoints with rate limiting
    location /api/ {
        limit_req zone=api_limit burst=20 nodelay;
        proxy_pass http://pola_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # All other requests (admin, etc.)
    location / {
        proxy_pass http://pola_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Health check
    location /health {
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
