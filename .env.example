# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_HOST=localhost
DB_PORT=5432

# API Configuration
API_VERSION=v1

# JWT Token Configuration (in minutes)
ACCESS_TOKEN_LIFETIME=60
REFRESH_TOKEN_LIFETIME=1440

# CORS Configuration
# Comma-separated list of allowed origins
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# CSRF Configuration
# Comma-separated list of trusted origins
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# Security Settings (Set to True in production)
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False

# Email (password reset OTP)
# Console backend prints emails to the server log (good for local dev)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@pola.co.tz
PASSWORD_RESET_OTP_TTL_MINUTES=15

# ==============================================================================
# AZAMPAY PAYMENT GATEWAY (Phase 4)
# ==============================================================================
# Get these credentials from: https://developers.azampay.co.tz/
AZAMPAY_APP_NAME=POLA
AZAMPAY_CLIENT_ID=your-azampay-client-id-here
AZAMPAY_CLIENT_SECRET=your-azampay-client-secret-here
AZAMPAY_API_KEY=your-azampay-api-key-here
AZAMPAY_ENVIRONMENT=sandbox
AZAMPAY_WEBHOOK_URL=http://localhost:8000/api/v1/subscriptions/webhooks/azampay/
